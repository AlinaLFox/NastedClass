package nasted;

public class CPU {
    public class Processor {}
    public class RAM{}

    CPU cpu = new CPU();
    CPU.Processor processor = cpu.new Processor();
    CPU.RAM ram = cpu.new RAM();
}
